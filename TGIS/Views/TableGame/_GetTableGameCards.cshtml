@model IEnumerable<TGIS.Models.TableGame>

@foreach (var item in Model)
{
    <div class="col-lg-2 col-md-3">
        <div class="card mt-4">
            <a href="@Url.Action("ShowTableGameDetail", "TableGame", new { tableGameID=item.ID})">
                <div class="card-body">
                    <img src="@Url.Action("GetPhotoBySourceID", "Photo", new { sourceID=item.ID, index=0})" class="img-fluid h-100" />
                </div>
                <div class="gameName">
                    @item.ChineseName<br />
                    @item.EnglishName
                </div>
            </a>
        </div>
    </div>
}

